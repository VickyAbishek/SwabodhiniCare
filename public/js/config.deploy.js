// scope: shared
// Deployment overrides for config.js, written by scripts/prepare-deploy.mjs at deploy time.
// Committed empty so a local checkout uses the defaults; never commit a real API_BASE here.
export const DEPLOY = {};
